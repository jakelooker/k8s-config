apiVersion: v1
kind: Service
metadata:
  labels:
    app: mosquitto
  name: mosquitto
  namespace: home-assistant
spec:
  ports:
    - name: port1
      port: 1883
      targetPort: 1883
  selector:
    app: mosquitto